---
import { Icon } from 'astro-icon';

export interface Props {
  icon: string;
}

const { icon } = Astro.props as Props;
---
<Icon name={icon} />

<style lang="css">
  svg {
    height: 60px;
    width: 60px;
  }
</style>
