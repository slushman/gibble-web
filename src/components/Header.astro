---
import SiteTitle from '@components/SiteTitle';
import { checkIsHome } from '@utils';

const { url } = Astro.request;
const isHome = checkIsHome(url);
---
{isHome && (
  <h1 class="text-dark"><SiteTitle /></h1>
)}

{!isHome && (
  <p class="text-dark"><SiteTitle /></p>
)}
